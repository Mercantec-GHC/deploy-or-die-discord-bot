name: deploy-or-die-discord-bot

services:
    discord-bot:
        build:
            context: .
            dockerfile: Dockerfile
        restart: unless-stopped
        env_file:
            - path: .env
        ports:
            - "3000:3000"